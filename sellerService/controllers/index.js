require('dotenv').config();

const {
    DB_USERNAME,
    DB_PASSWORD,
    DB_DATABASE,
    DB_HOST,
    DB_DIALECT
    } = process.env

const {Seller} = require('../models');

module.exports = {
    
}